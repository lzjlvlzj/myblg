extends back
block content
   form(action='/back/cms/img/upload', method='post', enctype="multipart/form-data")
       input(type='file',name="upfile",multiple="multiple")
       button(type='submit') 提交
       div(id='editor')
